# 19-当天中断后形成约10页父卡（CP-SAT）

本地连续硬约束实验：各列只允许按输入待学队列连续切片，不能跨天回头。

原有全部硬约束不变；输入队列不改；已完成和已取消任务不占未来位置。

软目标为每日接近C → 单格均衡 → 复习间隔 → 减少改动；连贯评分由硬约束保证为0。

每场景60秒共享预算、4个求解线程，独立进程；不读取以前CP-SAT的答案作为起点。

独立验收分类：超时未找到。未找到方案不等于无解。


## 异常与输入

- D1、D2 全部完成。
- D3 完成当天全局顺序前 50% 后中断并锁定实绩。
- 仍在 D3 当天，把当前 441-482 共 42 张子卡合并为一张约 10 页父卡。

复用原场景的事件边界快照，只重算该边界以后的计划；不代表已完成整条浏览器流程。
旧计划仅作为顺序、间隔和改动参考，不沿用旧版验收结论。

## 本次结果

- 状态：UNKNOWN；预算内尚未找到合法方案，不能称为无解。
- 已证明最优的软目标：0/5。
- 耗时：60132.9 ms。
- 新排起点：D4；C=32.000000 页；列数=5。
- 原因：尚未找到合法方案，不能称为无解。

未得到可展示的新计划；不拿旧计划填充本表。
