# 复习规划场景结果目录

本目录按规划模式分开保存生成结果，避免两种算法的表格和验收结论混用。

- `固定每日时长/`：现有容量优先算法的完整场景结果。每日目标容量固定，计划允许顺延。
- `固定总复习天数/`：固定结束日期算法的结果目录。剩余任务按剩余天数重新均分，尚待算法实现后生成。

两种模式使用同一组教材、卡片页数、异常事件和场景编号。算法结果、验收摘要和确定性摘要分别保存，不能直接复制另一种模式的结果作为通过证据。
